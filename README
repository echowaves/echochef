# initial minimal bootstrap on the server
# the next line migh not be needed if running on ec2
# sudo apt-get install ssh 
sudo apt-get install git-core

git clone git://github.com/echowaves/echochef.git
sudo ./echochef/install.sh